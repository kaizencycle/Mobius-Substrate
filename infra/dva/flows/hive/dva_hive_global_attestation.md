# DVA.HIVE — Global Attestation

## Purpose

Record, once per day (or more frequently), the global integrity posture:

- Cluster GI averages
- Node participation
- Major anomalies or drift events

The attestation is written to the Civic Ledger as `GLOBAL_GI_SUMMARY` for public accountability.
