global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "dva-lite"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - "dva-lite:8080" # replace with the reachable host:port for DVA.LITE
