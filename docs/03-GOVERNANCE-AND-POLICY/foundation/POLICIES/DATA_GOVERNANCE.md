# Data Governance

Attestations must be minimal, signed, and free of personal data. Datasets used for model work must declare license, provenance, and hashes (DVC/manifest). Unsigned datasets are rejected by CI.
