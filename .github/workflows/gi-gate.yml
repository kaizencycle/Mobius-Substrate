# MOBIUS-CLASS: integrity-gate
# OWNER: @kaizencycle
# INTRODUCED: C-148
# LAST-REVIEWED: 2026-01-05
# STATUS: active
# PURPOSE: Enforces Global Integrity (GI) baseline threshold

name: GI Gate (Integrity Threshold)

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - run: npm test --if-present

  gi-gate:
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - uses: actions/checkout@v4
      - name: GI baseline gate
        env:
          GI_BASELINE: ${{ vars.KAIZEN_GI_BASELINE || '0.993' }}
        run: |
          python3 - <<'PY'
          import os
          import sys

          gi = float(os.environ.get('GI_BASELINE', '0.0'))
          print(f"GI baseline = {gi}")

          if gi < 0.95:
              print(f"::error::GI baseline ({gi}) is below minimum threshold (0.95)")
              sys.exit(1)
          else:
              print(f"✅ GI baseline ({gi}) meets minimum threshold (0.95)")
              sys.exit(0)
          PY
