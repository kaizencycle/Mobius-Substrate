/**
 * Mobius SDK Example Service
 * Placeholder for SDK examples and demonstrations
 */

export const version = '0.1.0';

export function greet(name: string): string {
  return `Hello ${name} from Mobius SDK!`;
}
