# Kaizen Codex Lane

This repo is Codex-enabled for safe, auditable PRs from AI agents.

## How to use
1. Start a new Chat and paste the init block:

```
/codex init \
  --repo kaizencycle/Kaizen-OS \
  --branch main \
  --permissions write \
  --identity "Michael • Founding Core" \
  --reviewers "AUREA,EVE" \
  --labels "codex,auto-pr,needs-review" \
  --rules "GI>=0.95,no-secrets,tests-pass"
```
2. Authorize GitHub → grant read/write to this repo only.
3. Use the cheat-sheet commands in the doc.

## CI gates
- `codex-ci (test)` runs Node 20, installs deps, runs tests if present.
- `codex-ci (gi-gate)` enforces GI ≥ 0.95 (replace with real endpoint later).

## PR Template
Ensures: no secrets, CI green, GI attestation linked.

## Auto-merge (optional)
Enable `.github/auto-merge.yml` policy + branch protection for "main".

—
*This file is generated by the Kaizen Codex bootstrap.*
